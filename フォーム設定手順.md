# 面談予約フォームの受信設定手順（所要5分・無料）

LPの予約フォームを本稼働させるための設定。武田さんのGoogleアカウントでの操作が必要（Claudeでは代行不可）。

## 仕組み

```
応募者がLPのフォームを送信(名前・生年月日・学生or社会人・所属大学・メール)
  → 完了画面と自動返信メールからTimeRexでZoom面談を予約(予約は任意)
  → Google Apps Script(GAS)が受信
  → ①武田さんに通知メール ②応募者に受付完了+TimeRexリンクの自動返信
    ③スプレッドシート記録(任意)
```

※面談日程の確定・Zoom発行はTimeRex側で完結する。
※GAS未設定の間もフォームは動く（応募者のメールアプリが起動する簡易モード）。

## 手順

1. https://script.google.com を開き「新しいプロジェクト」
2. エディタの中身を全て消し、同フォルダの `gas-webhook.gs` の内容を貼り付けて保存（プロジェクト名は「下剋上インターンLPフォーム」など）
3. 右上「デプロイ」→「新しいデプロイ」→ 種類の歯車で「ウェブアプリ」を選択
   - 説明: 任意
   - 次のユーザーとして実行: **自分**
   - アクセスできるユーザー: **全員**
4. 「デプロイ」→ アクセス承認を求められたら自分のアカウントで許可
   （「このアプリは確認されていません」と出たら「詳細」→「安全ではないページに移動」で進む。自作スクリプトなので問題ない）
5. 表示された「ウェブアプリのURL」（https://script.google.com/macros/s/…/exec）をコピー
6. `index.html` 内の `const FORM_ENDPOINT = "";` のダブルクォートの間にそのURLを貼り付けて保存

## 動作テスト

LPを開き、CTAボタン → フォームに自分の情報を入力して送信。
以下の3点を確認：

- trixia.takedakouhei@gmail.com に通知メールが届く
- Googleカレンダーの第1希望日時に「【仮】下剋上インターン面談 〇〇」が入る
- 入力したメールアドレスに受付完了の自動返信が届く

## 応募をスプレッドシートにも記録したい場合（任意）

1. 新しいスプレッドシートを作成し、URLの `/d/` と `/edit` の間のIDをコピー
2. `gas-webhook.gs` の `SPREADSHEET_ID = ""` に貼り付け
3. GASエディタで保存 → 「デプロイ」→「デプロイを管理」→ 鉛筆マーク → バージョン「新バージョン」→ デプロイ

## 注意

- GASのコードを修正したら、必ず「デプロイを管理 → 新バージョン」で再デプロイしないと反映されない
- フォームのURL（FORM_ENDPOINT）は再デプロイしても変わらない
